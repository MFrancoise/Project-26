name: Job outputs and dependencies

on: 
  push:
    branches:
    - 'hotfix/**'
    
  pull_request:
    branches:
      - main:

  worflow_dispatch:

jobs: 
  generate-info
    runs_on: ubuntu-latest
    steps:
      - name: Purpose
        run: echo "Generating repository information..."

